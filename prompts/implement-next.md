Task {TASK_NUM_PREVIOUS} of {TASK_TOTAL} is complete and the page loads cleanly. Now do task {TASK_NUM} of {TASK_TOTAL}:

{TASK_TEXT}

**Important rules:**
- Complete ONLY this task. Do not start later tasks yet.
- Edit `index.html` (and `meta.json` if needed) in place. Do NOT rewrite from scratch — preserve everything from prior tasks.
- After this task, the page must still load without console errors.
- Keep additions in the appropriate `// === SECTION ===` block from your existing structure.

After completing this task, stop and wait for the next instruction.
